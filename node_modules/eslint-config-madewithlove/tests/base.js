for (let foo = 0; foo < 20; foo++) {
    // Foo
}

console.warn('Oh noes');
console.error('Oh noes');

const base = 'foo';
export default base;

const foo = 'bar';
switch (foo) {
    case 'bar':
        const baz = 'bar';

        return baz;

    default:
        return 'bar';
}
